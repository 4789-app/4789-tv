# Retired: the former v* tag trigger published debug-signed APKs without complete source.
# Replace the canonical root copy with publication/tv-release-candidate.yml.
name: Legacy TV publisher retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy publisher is retired. Use the signed candidate workflow and audited draft release procedure.'
          exit 1
